---
import Layout from '../layouts/Layout.astro'
import SectionContainer from '../components/SectionContainer.astro'
import ScrollEffect from '../components/ScrollEffect.astro'
import MarqueeContainer from '../components/MarqueeContainer.astro'
import { Globe } from '../components/MagicUI/Globe'
import BigWordsScroll from '../components/React/BigWordsScroll'
---

<Layout title='Welcome to Astro.'>
  <main>
    <SectionContainer class='py-8 2xl:pt-80 xl:pt-20 md:pt-56'>
      <h2
        class='leading-none text-center font-semibold mb-6 flex gap-x-2 items-center w text-[38px] xl:text-[84px] lg:text-[80px] md:text-[74px] sm:text-[58px]'>
        Historias que inspiran y conectan
      </h2>
      <p class='mb-4 text-xl max-w-[850px] text-center my-5 mx-auto'>
        Cada proyecto es una oportunidad para crear algo especial. Desde eventos
        hasta campañas de marca, capturo la esencia de cada momento.
      </p>
      <div class='grid grid-cols-4 grid-rows-5 gap-5'>
        <div class='col-span-2 row-span-2'>
          <video class='gift-video' src='./gifs-home-3.mp4' loop muted></video>
        </div>
        <div class='col-span-2 row-span-2 col-start-3'>
          <video class='gift-video' src='./gifs-home-4.mp4' loop muted></video>
        </div>
        <div class='col-span-2 row-span-3 row-start-3'>
          <video class='gift-video' src='./gifs-home-1.mp4' loop muted></video>
        </div>
        <div class='col-span-2 row-span-3 col-start-3 row-start-3'>
          <video class='gift-video' src='./gifs-home-2.mp4' loop muted></video>
        </div>
      </div>
    </SectionContainer>

    <BigWordsScroll client:load />

    <MarqueeContainer />

    <Globe client:load />

    <ScrollEffect text='Listo para crear algo increible' class='bg-[#0D0D0D]' />
  </main>
</Layout>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const videos = document.querySelectorAll('.gift-video')

    videos.forEach((video) => {
      video.addEventListener('mouseenter', () => {
        videos.forEach((v) => {
          if (v !== video) {
            v.pause()
          }
        })
        video.play()
      })
    })
  })
</script>
