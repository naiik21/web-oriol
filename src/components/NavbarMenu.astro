 <style>
    .navbar {
      position: fixed;
      top: 0;
      width: 100%;
      background: rgb(153 153 153 / 60%);
      backdrop-filter: blur(20px);
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      transition: all 0.3s ease;
      z-index: 1000;
    }


    .navbar.none {
      display: none;
    }

     .navbar.display {
      display:block;
    }

    .navbar ul {
      list-style: none;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 60px;
    }

    .navbar li {
      position: relative;
      margin: 0 15px;
      cursor: pointer;
    }

    .navbar a {
      text-decoration: none;
      color: #000000;
      font-size: 16px;
      font-weight: bold;
      padding: 5px 10px;
      transition: color 0.3s ease;
    }

    .navbar a:hover {
      color: #007bff;
    }

/* 
    .dropdown {
      display: none;
      position: absolute;
      top: 100%;
      left: 0;
      background: white;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      padding: 10px 0;
    }

    .dropdown li {
      padding: 5px 20px;
      white-space: nowrap;
    }

    .dropdown li a {
      font-size: 14px;
      color: #333;
    }

    .navbar li:hover .dropdown {
      display: block;
    }

 
    .content {
      margin-top: 60px; 
      padding: 20px;
    } */
  </style>

<div class="navbar display z-30 fixed w-full" id="navbar">
    <ul>
      <li>
        <a href="#">Inicio</a>
      </li>
      <li>
        <a href="../pages/aboutMe.astro">Sobre Mi</a>
      </li>
      <li>
        <a href="#">Trabajos</a>
      </li>
      <li>
        <a href="#">Contacto</a>
      </li>
    </ul>
</div>

<div class="navbar none z-30 fixed w-full" id="navbar-scroll">
    <ul>
        <li>
            <img src="./o-logo.png" alt="Logo" width="50px" height="50px">
            <div id="dropdownHover" class="z-10  rounded-lg absolute" style="left: 50%; transform: translate(-50%, 0%);  background: rgb(153 153 153 / 60%);
      backdrop-filter: blur(20px);">
                <ul class="py-2 text-sm text-gray-700 dark:text-gray-200" aria-labelledby="dropdownHoverButton">
                    <li class="static">
                        <a href="#" class="block px-4 py-2 hover:bg-gray-100 dark:hover:bg-gray-600 dark:hover:text-white">Inicio</a>
                    </li>
                    <li class="static">
                        <a href="../pages/aboutMe.astro" class="block px-4 py-2 hover:bg-gray-100 dark:hover:bg-gray-600 dark:hover:text-white">Sobre Mi</a>
                    </li>
                    <li class="static">
                        <a href="#" class="block px-4 py-2 hover:bg-gray-100 dark:hover:bg-gray-600 dark:hover:text-white">Trabajos</a>
                    </li>
                    <li class="static">
                        <a href="#" class="block px-4 py-2 hover:bg-gray-100 dark:hover:bg-gray-600 dark:hover:text-white">Contacto</a>
                    </li>
                </ul>
            </div>
        </li>
    </ul>

</div>
  
  <script>
    const navbar = document.getElementById('navbar');
    const navbarScroll = document.getElementById('navbar-scroll');

    window.addEventListener('scroll', () => {
      if (window.scrollY > 50) {
        navbar.classList.add('none');
        navbar.classList.remove('display');
        navbarScroll.classList.add('display');
        navbarScroll.classList.remove('none');
      } else {
        navbar.classList.add('display');
        navbar.classList.remove('none');
        navbarScroll.classList.add('none');
        navbarScroll.classList.remove('display');
      }
    });
  </script>